{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.1.1",
    "react-dom": "^16.1.1",
    "react-scripts": "1.0.17"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "clean": "rm -rf build",
    "deploy": "chmod +x ./bin/deploy.sh; ./bin/deploy.sh deploy",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}